import { externalImage } from '../../../helpers/external-image';
import { module, test } from 'qunit';

module('Unit | Helper | external image');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = externalImage(42);
  assert.ok(result);
});
